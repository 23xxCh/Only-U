# Copy to portable/.env on the USB stick. Never commit the real file.
DEEPSEEK_API_KEY=
# Optional AI Ping-compatible API endpoint. Leave empty to use the default.
# Example: https://aiping.cn/api/v1
DEEPSEEK_BASE_URL=
